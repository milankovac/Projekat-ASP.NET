@model Projekat.tblKategorija

@{
    ViewBag.Title = "Delete";
}




<div class="jumbotron">
    <h4>Izbrisati kategoriju</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Naziv)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Naziv)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Opis)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Opis)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

<div class="form-actions no-color">
    <a href="@Url.Action("Index", "Kategorija", new { id = Model.ID })" class="btn btn-success">
        <span class="glyphicon glyphicon-arrow-left" aria-hidden="true">
            Nazad
        </span>

    </a>
    <button type="submit" class="btn btn-danger">
        <span class="glyphicon glyphicon-trash" aria-hidden="true" onclick="return confirm ('Brisanjem ove kategorije obrisacete sve komade namestaja koji joj pripadaju!!!')">
            Izbriši
        </span>
    </button>
</div>
    }
</div>
